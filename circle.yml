machine:
  timezone:
    GMT
  xcode:
    version: 8.0
  environment:
    XCODE_SCHEME: nupic
    XCODE_WORKSPACE: nupic
    ARCHFLAGS: "-arch x86_64"
    PATH: "$HOME/Library/Python/2.7/bin:$PATH"
    PYTHONPATH: "$HOME/Library/Python/2.7/lib/python/site-packages:$PYTHONPATH"
    PIP: "$HOME/Library/Python/2.7/bin/pip"
  pre:
    - pip install --user --ignore-installed --upgrade --verbose virtualenv
    - ln -s ~/Library/Python/2.7/bin/virtualenv /usr/local/bin/virtualenv
  post:
    - pip install --user --ignore-installed --upgrade --verbose pip

dependencies:
  pre:
    - echo "$PATH"
    - echo "$PYTHONPATH"
    - ls -la $HOME/Library/Python/2.7/lib/python/site-packages/
    - echo $PIP
    - $PIP --version
    - $PIP install --user wheel
    - $PIP install --user numpy==1.11.2 pycapnp==0.5.8
  override:
    - mkdir -p build/scripts
    - cd build/scripts && cmake ../.. -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=../release -DPY_EXTENSIONS_DIR=../../bindings/py/nupic/bindings

#database:
#compile:
test:
  override:
    - py.test tests/unit
    - py.test tests/integration
