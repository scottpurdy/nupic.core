machine:
  timezone:
    UTC
  xcode:
    version: 8.0
  environment:
    XCODE_SCHEME: nupic
    XCODE_WORKSPACE: nupic
  override:
    echo "no virtualenv"

#dependencies:
#database:
#compile:
#test:
